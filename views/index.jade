extends layout

block content
  h1= title
  p #{content}
  if imageURL == undefined
  else
   img(src='#{imageURL}')

  if pageURL == undefined
   script.
    history.replaceState( null, null, 'https://notemaker-server.herokuapp.com/notes');
  else
   script.
     history.replaceState( null, null, #{pageURL});
    